#include <stdio.h>
int main()
{
    int length,breadth;
    int area;
    printf("Enter the value of length :\n");
    scanf("%d",&length);
    printf("Enter the value of length :\n");
    scanf("%d",&breadth);
    area = length * breadth;
    printf("Area of rectangle is :%d",area);
return 0;
}